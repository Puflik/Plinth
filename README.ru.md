# Plinth

*English version: [README.md](README.md)*

Автономный Android-плеер, в котором **трек — логическая сущность, а не файл
и не ссылка**. У одного трека может быть несколько источников: локальный файл,
онлайн-поток, покупка, запись из архива — приложение само выбирает лучший
доступный в текущих условиях. Разделения «онлайн/оффлайн» здесь нет: оно было
следствием того, что в других плеерах трек равен источнику.

Центрального бэкенда нет. «Сервер» — само устройство, десктоп равноправен,
синхронизация — опциональная надстройка над журналом операций.

## Статус

**Pre-alpha. Устанавливать пока нечего.**

Текущая волна — **v0.1 «Звук»**: плеер локальных файлов. Уже сделано:

- аудиоядро: абстракция плеера и реализация на Media3 за ней, фоновое
  воспроизведение с медиасессией, управление из уведомления и гарнитуры;
- библиотека из системного индекса медиафайлов: треки, альбомы, исполнители
  и папки, поиск, выбор папок для сканирования;
- очередь с перемешиванием, повтором и ручными добавлениями, которая
  переживает перезапуск;
- экран плеера, мини-плеер, правка очереди и жесты;
- мастер первого запуска и адаптивный стартовый экран;
- английский и русский языки;
- надёжность: локальный лог без личных данных, отчёты о сбоях, которые вы сами
  решаете сохранить, подсказка, когда прошивка убивает фоновую игру, и пропуск
  файлов, которые не играют.

До первого выпуска осталась приёмка. Онлайн-источники, один трек из нескольких
источников и синхронизация между устройствами — в следующих волнах.

| | |
|---|---|
| Package ID | `io.github.puflik.plinth` |
| Минимальный Android | 8.0 (API 26) |
| Собирается против | API 37 |
| Распространение | GitHub Releases и F-Droid. **Не** Google Play |
| Лицензия | [AGPLv3](LICENSE) |

## Сборка

Что нужно:

- **JDK 21 или новее.** Подходит тот, что лежит в Android Studio —
  `<studio>/jbr` (JDK 25); в терминале укажите на него `JAVA_HOME`. CI
  собирает на Temurin 21. На JDK 8 Gradle не запустится.
- **Android SDK.** Путь к нему — в `local.properties`, строка `sdk.dir=...`
  (Android Studio пишет этот файл сама). Недостающие платформы сборка скачает.

Из одних исходников собираются два flavor:

| Flavor | Проверка обновлений |
|---|---|
| `github` | включена — вне магазина автообновлений нет |
| `fdroid` | выключена — F-Droid обновляет приложения сам |

```bash
./gradlew assembleGithubDebug        # APK, flavor github
./gradlew testGithubDebugUnitTest    # unit-тесты
./gradlew ktlintCheck detekt         # стиль и статический анализ
```

`ktlintFormat` правит форматирование на месте.

## Участие и документация

- [CONTRIBUTING.md](CONTRIBUTING.md) — сборка, какие правки принимаются, как
  пишется код (по-английски).
- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — пакеты, границы и потоки
  данных; [docs/adr/](docs/adr/) — ключевые решения и почему.

## Лицензия

GNU Affero General Public License v3.0 — текст в [LICENSE](LICENSE).

Цель сформулирована прямо: никто не должен суметь форкнуть проект, сделать
закрытую экосистему и выложить её продаваться.
