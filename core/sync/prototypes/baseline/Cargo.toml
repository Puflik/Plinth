# Точка отсчёта для веса в APK: та же нагрузка и проекция, но без CRDT.
[package]
name = "proto-baseline"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
proto-common.workspace = true
