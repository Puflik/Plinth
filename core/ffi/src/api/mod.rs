//! API ядра для Kotlin (A3.1) — методы [`crate::Core`] по темам. Сканер
//! (`scan_api`) придёт с D1, единый поиск и выбор источника (`search_api`,
//! `source_api`) — с провайдерами в эпике E.

mod journal_api;
mod library_api;
