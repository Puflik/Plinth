# Project decisions & answers

Persisted answers to questionnaires and key project decisions for Plinth.
Read this first for prior context. Claude appends here per the CLAUDE.md
"Recording answers & decisions" rule, then folds it into the graph.

## 2026-09-21

- **Кто делает коммиты.** Claude **никогда** не выполняет `git commit` и не создаёт коммиты
  любым другим способом. Коммитит только автор — лично, после того как сам проверит файлы,
  даже если Claude уже их проверил. Claude готовит изменения в рабочем дереве, показывает
  diff, предлагает текст коммита; команду выполняет автор.
- **Структура git-репозитория.** Работа велась в двух вложенных репозиториях: пустой
  `git init` в `C:\claude\Plinth` (ветка `master`, 0 коммитов, без remote) и клон
  `Puflik/Plinth` в подпапке `Plinth/`. Все документы лежали в пустом. Исправлено:
  пустой репозиторий удалён, клон поднят в корень проекта, `LICENSE`, `README.md`
  и `.gitattributes` перенесены наверх, подпапка убрана. Рабочая ветка — `main`,
  remote — `https://github.com/Puflik/Plinth.git`. Сам репозиторий на GitHub создан
  верно (AGPL-3.0, имя, ветка) и не пересоздавался.
- **`.gitignore` и `.gitattributes` доведены до рабочего состояния.** Игнорируются
  Gradle/Android-артефакты, `target/` Rust, IDE, `graphify-out/` и — отдельным блоком —
  **файлы подписи** (`*.jks`, `*.keystore`, `keystore.properties`). `.gitattributes`
  фиксирует `gradlew` как LF, `*.bat`/`*.cmd` как CRLF и помечает бинарные типы,
  включая `gradle-wrapper.jar`.

## 2026-09-22

- **Причина «проект не работает».** Красный треугольник и ошибки в чате
  «Пет-проект директория» — это лимит сессии, а не поломка папки: контекст того
  чата дорос до ~300 тыс. токенов на сообщение (124 хода, 23,3 млн токенов
  чтения из кэша, три фоновых субагента graphify). Папка, git, хуки, graphify
  проверены — исправны. Доплата за превышение отключена на уровне организации,
  запаса при достижении лимита нет.
- **`plan.md` разбит по главам.** 2288 строк (~55 тыс. токенов) разложены в
  `docs/plan/` — 27 файлов по главам плюс `README.md` с оглавлением; склейка
  проверена побайтово. Сам `plan.md` оставлен точкой входа с таблицей ссылок.
- **Предохранитель на чтение.** `.claude/hooks/guard-big-docs.py` (PreToolUse на
  `Read` и `Bash`) запрещает читать `.md`/`.txt`/`.rst` тяжелее ~9 тыс. токенов
  целиком и подсказывает, как взять нужный кусок. Порог подобран так, чтобы
  главы плана проходили, а `tasks*.md` — нет.
- **Разбор плана из оборвавшейся сессии сохранён** в
  `docs/plan-review-2026-09-21.md`; замечания раздела «Дыры» ждут ответов автора.
- **Бюджет чата закреплён правилом и хуком.** Тяжёлые операции (субагенты,
  `/graphify` с экстракцией, чтение крупных документов целиком) — только
  отдельным чатом; новый чат при смене задачи и каждые 30–50 сообщений.
  `.claude/hooks/chat-budget.py` считает контекст по транскрипту: с 80 тыс.
  токенов предупреждает, со 120 тыс. требует перейти в новый чат, с 60 тыс.
  запрещает `Task` и тяжёлые скиллы.
- **`tasks*.md` — не дубликаты `plan.md`.** Это его детализация по волнам,
  доведённая пока не до всех версий. Сводить или удалять нечего; когда файлы
  дорастут, разбивать их так же, как план.
- **Шумные хуки ужаты.** Убрано напоминание про graphify на каждом `Read`/`Glob`
  (правило переехало в CLAUDE.md и в `guard-big-docs.py`), Bash-напоминание
  сжато со ~120 до ~20 токенов на впрыск, впрыск стоимости отключён через
  `ECC_CONTEXT_MONITOR_COST_WARNINGS=false` в `.claude/settings.json`
  (предупреждения об остатке контекста остались).
- **Правки хуков применяются без перезапуска чата** — проверено на этом же
  сжатом хуке.
- **Окружение сессии почищено.** Плагин `ecc` отключён для этого проекта
  (`enabledPlugins` в `.claude/settings.json`) — минус ~17 тыс. токенов
  системного промпта и 61 инструмент MCP (github, playwright, memory, exa,
  context7, sequential-thinking). Отключены коннекторы Figma, visualize и
  Claude Docs — ещё 29 инструментов; включаются обратно одним переключателем.
  Остаётся `scheduled-tasks` (6 инструментов, коннектором не управляется).
- **Нужные скиллы перенесены из плагина.** В проект (`.claude/skills/`, только
  для Plinth): `rust-patterns`, `rust-testing`, `kotlin-patterns`,
  `kotlin-coroutines-flows`, `android-clean-architecture`, `tdd-workflow`,
  `git-workflow`. Глобально (`~/.claude/skills/`): `plan-prd` — единственный
  скилл ecc, которым реально пользовались.


- **Плагин `ecc` на самом деле не был отключён.** В `.claude/settings.json` и в
  `~/.claude/settings.json` стоял несуществующий идентификатор
  `everything-claude-code@everything-claude-code`; реальный — `ecc@ecc`
  (маркетплейс `ecc`, `~/.claude/plugins/marketplaces/ecc`). Из-за опечатки
  флаг ни на что не влиял: сессия грузила ~80 агентов `ecc:*`, ~250 скиллов и
  6 MCP-серверов `plugin:ecc:*` (61 инструмент). В проектном
  `.claude/settings.json` ключ исправлен на `"ecc@ecc": false`; подействует
  со следующего чата.
- **Стартовая цена чата измерена.** С загруженным `ecc` чат открывается уже
  с ~122 тыс. токенов контекста — выше жёсткого порога `chat-budget.py`
  (120 тыс.). Вывод: кодить начинаем в свежем чате, уже без плагина.
- **`.claude/skills/` исключены из индексации graphify.** Создан
  `.graphifyignore` (graphify читает его наравне с `.gitignore`). Граф
  пересобран через `graphify update .` (AST, без API): 977 → 728 узлов,
  минус 249 узлов скилловых SKILL.md — четверть графа была шумом. Записи о
  скиллах вычищены и из `graphify-out/manifest.json`.
- **Хуки проверены вживую.** `guard-big-docs.py` блокирует `Read` целиком и
  `cat` для `.md` тяжелее ~9 тыс. токенов, срез с `offset/limit` пропускает;
  `chat-budget.py` корректно считает контекст по транскрипту.

### Решения перед стартом волны v0.1 «Звук»

- **DI — Hilt** (закрывает A2.4, идёт в ADR `0004`). Аргумент тот же, что выбрал
  Kotlin с Compose: стандарт Android, огромный корпус примеров, AI-агент реже
  ошибается. Бесплатно даёт внедрение в `ViewModel` и `MediaSessionService` и
  подмену реализаций в тестах (`@TestInstallIn` — подставить `FakeAudioEngine`
  вместо `Media3Engine`). Цена — KSP-кодогенерация и более медленная сборка.
  Ручной граф чище и легче, но проводку `ViewModel`/`Service` пришлось бы
  писать руками, а с ростом графа в v0.2–v0.3 это превращается в рутину.
- **Тесты Media3 — на эмуляторе** (`androidTest`), не Robolectric.
- **Первая вертикаль берёт SAF (`ACTION_OPEN_DOCUMENT`), не `MediaStore`.**
  SAF не требует runtime-разрешений и работает с любой папкой, поэтому
  вертикаль «файл → звук → фон» обходится без эпика C целиком (разрешения C1,
  сканер C2, Room C3). `MediaStore` остаётся основой сканера (C2.1) и приходит
  следующей вертикалью — он даёт всю фонотеку с разобранными тегами, но требует
  `READ_MEDIA_AUDIO` и видит только проиндексированное системой.
- **Первая вертикаль — «звук из файла», по шагам 1–4 порядка работ:**
  1. A1 + A1.3 + A2 — каркас, два flavor сразу, `minSdk 26`/`compileSdk 36`;
     зелёный критерий: `assembleGithubDebug` и пустой unit-тест проходят.
  2. B1 по TDD: сначала `AudioEngineContractTest`, затем интерфейс, модели и
     `FakeAudioEngine`. Плюс тест-страж: ни одного импорта `androidx.media3`
     в `audio/engine/**`.
  3. B2: `Media3EngineContractTest` наследует тот же контракт, затем
     `ExoPlayerFactory`, `MediaItemMapper`, `PlayerListenerAdapter`,
     `Media3Engine`. Прогон семи форматов (B2.4) и gapless — позже.
  4. B3 минимум: `PlaybackService`, `NotificationChannels`,
     `PlaybackController`; затем тонкий UI — SAF-выбор файла и play/pause/seek,
     `ViewModel` тестируется на `FakeAudioEngine`.
  Вне вертикали: эпики C, D, E, F, G, H.
- **Окружение сборки.** `java` на PATH — JDK 8, для Gradle указывать JBR 21 из
  Android Studio (`C:\Program Files\Android\Android Studio\jbr`). В SDK стоит
  только `android-36`, build-tools 36.0.0.
- **Пустая папка `C:\claude\Plinth\Plinth`** — мёртвая запись от переноса
  репозитория, в хранилище GitHub Desktop прописана наравне с настоящим
  `C:\claude\Plinth`. Внутри нет `.git`. Убирает автор: в GitHub Desktop
  Remove записи, затем удалить папку.

- **Что уходит в публичный репозиторий.** Коммитятся план и декомпозиция
  (`plan.md`, `docs/plan/`, `tasks*.md`), `docs/decisions.md`, `CLAUDE.md`,
  `.graphifyignore`, а также `.claude/settings.json` и `.claude/hooks/` —
  это правила, без которых репозиторий не работает с агентом. Секретов в них
  нет. Не коммитятся: `graphify-out/` (пересобирается), `.claude/skills/`
  и `.claude/settings.local.json`.
- **Почему `.claude/skills/` не коммитятся.** Это копии скиллов из плагина
  `ecc` (MIT, (c) Affaan Mustafa), перенесённые без текста лицензии и
  копирайта, чего MIT требует от копий. Класть их в AGPL-репозиторий нельзя;
  каждый ставит себе сам.
- **В `.gitignore` были две молчаливые ошибки.** Строка `.claude` игнорировала
  весь каталог, включая хуки и `settings.json`, вопреки собственному
  комментарию. Строка `Claude.md` при `core.ignorecase=true` на Windows
  игнорировала `CLAUDE.md` — главный файл правил не попадал в репозиторий и
  никак об этом не сообщал. Блок переписан на `.claude/*` с точечными
  исключениями.
- **Перед первым коммитом — `git add --renormalize .`** `LICENSE` и
  `README.md` числятся изменёнными с пустым diff: новый `.gitattributes`
  (`* text=auto`) требует перенормировать окончания строк. Содержимое то же.

### Шаг 1 вертикали «звук из файла»: каркас A1 + A1.3 + A2

- **Тулчейн взят актуальный, а не установленный.** AGP **9.4.1**, Gradle **9.7.1**,
  `compileSdk`/`targetSdk` **37**, Kotlin 2.3.21, KSP 2.3.12, Hilt 2.60.1,
  Compose BOM 2026.09.00, Media3 1.11.1. Причина: библиотеки AndroidX выпуска
  2026 (`compose-ui` 1.12, `core-ktx` 1.19) требуют AGP 9.1+ и compileSdk 37,
  а Hilt с версии **2.59** отказывается работать с AGP 8. Оставаться на
  AGP 8.12 означало бы пригвоздить проект к Hilt 2.58 и библиотекам осени 2025
  в первый же день. Решение автора: «на то, что установлено, не обращай
  внимания — бери тот вариант, что лучше».
  **Следствие:** установленная Android Studio 2025.1.2 несёт AGP 8.12 и этот
  проект не откроет — Studio нужно обновить. Платформу `android-37` сборка
  скачивает сама, вручную ставить не нужно.
  Отменяет прежнюю запись «`compileSdk 36`» в решениях перед стартом волны.
- **Плагин `org.jetbrains.kotlin.android` не применяется.** С AGP 9.0 поддержка
  Kotlin встроена в сам AGP, и отдельный плагин он запрещает явной ошибкой.
  В сборке остались `com.android.application`, `kotlin.plugin.compose`, `ksp`
  и `hilt.android`.
- **Форматирование — только ktlint.** Набор `detekt-formatting` снят: он несёт
  свою, более старую копию правил ktlint, и два линтера начинают спорить об
  одних и тех же отступах (поймано на цепочке `Modifier`). Зоны разведены:
  ktlint + `.editorconfig` — стиль, detekt — запахи кода и сложность.
- **`ui/navigation/Destinations.kt` переименован в `Destination.kt`** — правило
  ktlint `standard:filename`. Декомпозиция в `tasks-v0.1.md` поправлена.
- **`AudioModule` и `LibraryModule` (A2.4) не созданы.** Привязывать пока нечего:
  `AudioEngine` появляется на шаге 2, репозитории — в эпике C. `AppModule`
  выдаёт диспетчеры (`@IoDispatcher`, `@DefaultDispatcher`) через граф, а не
  через обращение к `Dispatchers` по месту, — иначе в тестах их нечем подменить
  на `TestDispatcher`.
- **`PlaybackService` в манифесте не объявлен** — появится вместе со своим
  классом (B3.1). Разрешения фонового воспроизведения объявлены уже сейчас:
  состав разрешений — ответственность A2.1, и все они нужны этой же вертикали.
- **Подпись релиза готова, но выключена.** `signingConfigs` создаётся только
  если есть `keystore.properties` или переменные `PLINTH_KEYSTORE_*`; без них
  `assembleGithubRelease` даёт неподписанный APK. `release.yml` написан целиком
  и включится в H5, когда появится ключ.
- **Gradle wrapper собран вручную.** В `~/.gradle/wrapper/dists` лежал битый
  (недокачанный) дистрибутив 8.13, `gradle` в PATH нет. Файлы wrapper взяты из
  локального проекта Android Studio, `distributionUrl` переведён на 9.7.1.
- **`local.properties`: путь пишется прямыми слэшами.** В формате `.properties`
  одиночный обратный слэш съедается, путь вида `C:\Users\...` превращается в
  `C:Users...`, и AGP падает с «Синтаксическая ошибка в имени файла».
- **TDD по `tdd-workflow`, но без чекпойнт-коммитов скилла.** RED зафиксирован
  компиляционной ошибкой (`Unresolved reference 'FlavorConfig'`,
  `'ApiKeySource'`, `'Destination'`), GREEN — прогоном тестов. Коммиты скилла
  не делаются: в этом репозитории коммитит только автор.
- **Зелёный критерий шага 1 достигнут.** От `clean`: `ktlintCheck`, `detekt`,
  `testGithubDebugUnitTest` и `testFdroidDebugUnitTest` (по 11 тестов на flavor,
  0 падений), `assembleGithubDebug`, `assembleFdroidDebug`,
  `assembleGithubRelease`.
- **Сборке нужен JDK 21.** `java` в PATH — JDK 8; в терминале перед `./gradlew`
  надо выставить `JAVA_HOME` на JBR из Android Studio. Записано в обоих README.
- **Как запускать сборку из терминала.** Форма `JAVA_HOME=... ./gradlew ...`
  работает только в bash. В PowerShell (терминал по умолчанию) префикса
  переменной перед командой нет, нужно:
  `$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"; .\gradlew.bat assembleGithubDebug`
  Проверено — собирается.
- **`ARCHITECTURE.md` и ADR (A3) на шаге 1 не заводились**, решение автора
  ожидается. Обоснование Hilt и остальных выборов уже лежит здесь же, в
  `docs/decisions.md`, так что ADR `0004` сейчас был бы дубликатом.
